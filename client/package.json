{
  "name": "@axhub/make-client",
  "version": "0.1.0",
  "private": true,
  "description": "Official Axhub Make client runtime",
  "type": "module",
  "scripts": {
    "vendor:sync": "pnpm --filter @axhub/make vendor:sync",
    "dev": "pnpm vendor:sync && vite",
    "start": "pnpm dev",
    "metadata:sync": "node scripts/sync-project-metadata.mjs",
    "build": "pnpm vendor:sync && node scripts/scan-entries.js && node scripts/build-all.js",
    "preview": "vite preview",
    "typecheck": "pnpm vendor:sync && tsc --noEmit -p tsconfig.typecheck.json",
    "test": "pnpm test:run",
    "test:run": "pnpm vendor:sync && vitest --run",
    "test:coverage": "pnpm vendor:sync && vitest --run --coverage",
    "test:watch": "pnpm vendor:sync && vitest",
    "test:ui": "vitest --ui",
    "coverage": "pnpm test:coverage",
    "check-ready": "node scripts/check-app-ready.mjs",
    "capture:theme": "node scripts/capture-theme-homepage.mjs"
  },
  "devDependencies": {
    "@floating-ui/react": "^0.27.17",
    "@radix-ui/react-dropdown-menu": "^2.1.16",
    "@radix-ui/react-popover": "^1.1.15",
    "@tailwindcss/vite": "^4.1.18",
    "@vitejs/plugin-react": "^5.0.0",
    "@vitest/coverage-v8": "4.0.16",
    "@vitest/ui": "4.0.16",
    "echarts": "^6.0.0",
    "extract-zip": "^2.0.1",
    "iconv-lite": "^0.7.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.97.3",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3",
    "vite": "^5.0.0",
    "vitest": "^4.0.16",
    "ws": "^8.18.3"
  },
  "dependencies": {
    "@axhub/annotation": "^1.0.0",
    "lucide-react": "^0.562.0",
    "motion": "^12.38.0"
  }
}
